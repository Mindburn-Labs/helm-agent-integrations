# LlamaIndex + HELM

LlamaIndex tools and MCP calls can route through HELM before side effects.

Recommended first example:

```text
MCP search or tool call discovers an unapproved side-effectful tool.
HELM quarantines before dispatch.
```
