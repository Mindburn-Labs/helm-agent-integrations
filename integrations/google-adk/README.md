# Google ADK + HELM

ADK can build and deploy agents while HELM governs side-effect execution.

Recommended first example:

```text
Procurement agent proposes a vendor purchase.
HELM escalates because the side effect needs approval.
```
